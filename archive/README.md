# Archive

This folder contains preserved legacy material only.

`investigation-legacy-2026-08-21/` is the blank-slate reset snapshot of the pre-existing investigation surface. It preserves the exact source trees that were active immediately before the reset, including remaining Babbler and one-shot material that still requires the separately queued migration into `louisgiles/oneshots`.

Nothing under `archive/` is an active investigation query or approved for reactivation by default. Use it as reference material only. Do not delete Babbler or one-shot source from this archive until the destination copy has been verified.
